- cssclass = "address "
- cssclass << address.address_type.slug if address.address_type

%li{class: cssclass}
  %svg.icon.small
    - icon = address.address_type ? address.address_type.slug : "correspondence"
    %use{"xlink:href" => "#address_type_#{icon}_symbol"}
  %span.contact
    = address.address
