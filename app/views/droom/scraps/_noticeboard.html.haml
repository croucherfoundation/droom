- refresh_url ||= droom.scraps_path(format: :js)
#noticeboard.autogrid{:data => {:refreshing => refresh_url}}
  - if @scraps.any?
    = render :partial => "droom/scraps/notice", :collection => @scraps

  - else
    = t :no_notices
  