- content_for :pagetitle do 
  Caledar view 

- content_for :breadcrumb do
  = render "droom/events/calendar/breadcrumb"

.calendar_container
  = render 'droom/events/calendar/toolbar'

  -# JS will populate this div with the calendar view
  #calendar-view.calendar-view{ data: { events: events_for_calendar(@events) } }
    
  = render 'droom/events/calendar/dialog'

