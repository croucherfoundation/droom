#document
  - if @view == 'google_doc'
    - title = t(:edit_google_doc)
  - elsif @view == 'notion_page'
    - title = t(:edit_notion_page)
  - else
    - title = t(:edit_document)
    
  = render :partial => 'form', locals: {title: title}