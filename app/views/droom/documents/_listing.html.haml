- listing ||= nil
- omit_menu ||= false
- document ||= listing || @document
- narrow ||= false
- character_count = narrow ? 28 : 64
- url = emergency_mode? ? document.file.url : droom.folder_document_url(document.folder, document)

%li.document{:id => "document_#{document.id}", data: {narrow: narrow, doc_id: document.id}}
  = link_to shorten(document.name, character_count, ''), url, class: "document #{document.file_extension.downcase}", target: "_blank"
  - unless omit_menu
    = action_menulink(document)
  = action_menu(document)
