- tags ||= []
- space_top ||= false
- tagclass ||= ''

- if tags.any?
  .croucher_tags.mt_10{class: ("mt_30" if space_top)}
    - tags.each do |tag|
      .croucher_tag{class: tagclass}
        = tag.name
